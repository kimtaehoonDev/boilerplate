server:
  port: 8080
spring:
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: none

test:
  str: 문자열
  ary: [ 1, 10, 100 ]

---
### Local 운영환경 ###
spring:
  config:
    activate:
      on-profile: local